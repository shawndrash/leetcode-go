# LeetCode Go 项目说明

## 🎯 项目目标

这是一个专门为 Go 语言刷 LeetCode 设计的项目模板，具有以下特点：

1. ✅ **题目隔离**：每道题使用独立的包，避免命名冲突
2. ✅ **便捷测试**：完整的单元测试和性能测试支持
3. ✅ **工具复用**：提供链表、树等常用数据结构的工具类
4. ✅ **自动化**：一键生成新题目模板
5. ✅ **简化操作**：Makefile 提供便捷命令

## 📦 完整目录结构

```
leetcode-go/
├── README.md                    # 项目主文档
├── 快速开始指南.md               # 快速上手指南
├── 项目说明.md                  # 本文件
├── go.mod                       # Go 模块定义
├── .gitignore                   # Git 忽略文件
├── Makefile                     # 便捷命令
├── new_problem.sh              # 新建题目脚本
│
├── problems/                    # 所有题目
│   ├── 0001.two-sum/           # 第1题：两数之和（哈希表示例）
│   │   ├── README.md           # 题目描述
│   │   ├── solution.go         # 解法实现
│   │   └── solution_test.go    # 测试用例
│   │
│   ├── 0002.add-two-numbers/   # 第2题：两数相加（链表示例）
│   │   ├── README.md
│   │   ├── solution.go
│   │   └── solution_test.go
│   │
│   └── 0104.maximum-depth-of-binary-tree/  # 第104题：二叉树最大深度（树示例）
│       ├── README.md
│       ├── solution.go
│       └── solution_test.go
│
└── utils/                       # 工具包
    ├── listnode.go             # 链表工具（构建、比较、转换等）
    ├── treenode.go             # 树工具（构建、比较、转换等）
    └── helpers.go              # 辅助函数（Max、Min、切片操作等）
```

## 🚀 核心功能

### 1. 独立包结构

每道题使用独立的 Go 包，格式为：`题号.题目名`

**优点**：
- ✅ 不同题目的函数名可以重复，不会冲突
- ✅ 便于管理和查找
- ✅ 每题可以有多个解法对比

**示例**：
```
problems/0001.two-sum/          -> package two_sum
problems/0002.add-two-numbers/  -> package add_two_numbers
```

### 2. 完整的测试支持

每道题包含：
- **单元测试**：验证解法正确性
- **性能测试**：比较不同算法的性能
- **测试覆盖率**：确保代码质量

**运行方式**：
```bash
# 测试单题
make test NUM=1

# 测试所有题
make test-all

# 性能测试
make bench NUM=1
```

### 3. 通用工具类

#### 链表工具 (`utils/listnode.go`)

```go
// 构建链表
list := utils.BuildList([]int{1, 2, 3})

// 链表转切片
slice := utils.ListToSlice(list)

// 比较链表
equal := utils.ListEqual(list1, list2)

// 打印链表（调试）
utils.PrintList(list)

// 其他工具
length := utils.GetLength(list)
node := utils.GetNodeAt(list, 2)
```

#### 树工具 (`utils/treenode.go`)

```go
// 构建树（-1 表示 nil）
tree := utils.BuildTree([]int{1, 2, 3, -1, -1, 4, 5})

// 树转切片
slice := utils.TreeToSlice(tree)

// 比较树
equal := utils.TreeEqual(tree1, tree2)

// 打印树（调试）
utils.PrintTree(tree)

// 其他工具
height := utils.GetHeight(tree)
count := utils.CountNodes(tree)
```

#### 辅助函数 (`utils/helpers.go`)

```go
// 数学运算
max := utils.Max(a, b)
min := utils.Min(a, b)
abs := utils.Abs(x)

// 切片操作
equal := utils.IntSliceEqual(slice1, slice2)
contains := utils.IntSliceContains(slice, target)
reversed := utils.ReverseIntSlice(slice)
copied := utils.CopyIntSlice(slice)
```

### 4. 自动生成题目模板

使用脚本一键生成题目结构：

```bash
# 方法1：直接使用脚本
./new_problem.sh 3 longest-substring "无重复字符的最长子串"

# 方法2：使用 Makefile
make new NUM=3 NAME=longest-substring CNAME="无重复字符的最长子串"
```

**自动生成**：
- ✅ solution.go - 带有函数签名
- ✅ solution_test.go - 测试模板
- ✅ README.md - 题目描述模板

### 5. Makefile 简化操作

无需记忆复杂的 Go 命令，使用简单的 make 命令：

```bash
make help              # 显示所有命令
make test NUM=1        # 测试第1题
make test-verbose NUM=1 # 详细测试
make test-all          # 测试所有题
make bench NUM=1       # 性能测试
make coverage NUM=1    # 覆盖率
make list              # 列出所有题目
make fmt               # 格式化代码
make clean             # 清理缓存
```

## 📝 工作流程

### 开始做新题

1. **创建题目**
   ```bash
   make new NUM=206 NAME=reverse-linked-list CNAME="反转链表"
   ```

2. **实现解法**
   编辑 `problems/0206.reverse-linked-list/solution.go`

3. **编写测试**
   编辑 `problems/0206.reverse-linked-list/solution_test.go`

4. **运行测试**
   ```bash
   make test NUM=206
   ```

5. **性能测试**
   ```bash
   make bench NUM=206
   ```

### 示例：两数之和

```go
// problems/0001.two-sum/solution.go
package two_sum

func twoSum(nums []int, target int) []int {
    numMap := make(map[int]int)
    for i, num := range nums {
        if j, ok := numMap[target-num]; ok {
            return []int{j, i}
        }
        numMap[num] = i
    }
    return nil
}
```

```go
// problems/0001.two-sum/solution_test.go
package two_sum

import "testing"

func TestTwoSum(t *testing.T) {
    tests := []struct {
        nums   []int
        target int
        want   []int
    }{
        {[]int{2, 7, 11, 15}, 9, []int{0, 1}},
        {[]int{3, 2, 4}, 6, []int{1, 2}},
    }

    for _, tt := range tests {
        got := twoSum(tt.nums, tt.target)
        // 验证结果...
    }
}
```

## 🎓 设计理念

### 为什么每题一个独立包？

**问题**：如果所有题目在同一个包中，会出现：
- ❌ 函数名冲突（很多题都叫 `maxDepth`、`reverse` 等）
- ❌ 测试文件混乱
- ❌ 难以管理

**解决方案**：每题独立包
- ✅ 函数名可以与 LeetCode 完全一致
- ✅ 测试独立运行，互不影响
- ✅ 清晰的目录结构

### 为什么提供工具类？

**问题**：很多题目需要链表、树等数据结构，每次都要：
- ❌ 重复定义 `ListNode`、`TreeNode`
- ❌ 重复编写构建、比较函数
- ❌ 测试代码冗长

**解决方案**：统一的工具包
- ✅ 一次定义，处处使用
- ✅ 丰富的辅助函数
- ✅ 简化测试代码

### 为什么使用 Makefile？

**问题**：Go 测试命令繁琐：
- ❌ `cd problems/0001.two-sum && go test -v`
- ❌ `go test -bench=. -benchmem ./problems/0001.two-sum`

**解决方案**：简化为：
- ✅ `make test NUM=1`
- ✅ `make bench NUM=1`

## 📊 示例题目

项目包含 3 个完整示例：

| 题号 | 题目 | 类型 | 演示内容 |
|------|------|------|----------|
| 0001 | 两数之和 | 哈希表 | 基本结构、测试编写 |
| 0002 | 两数相加 | 链表 | 链表工具使用 |
| 0104 | 二叉树最大深度 | 树 | 树工具使用、多解法对比 |

## 🔧 技术栈

- **语言**：Go 1.21+
- **测试**：Go Testing 包
- **构建**：Make
- **版本控制**：Git

## 📚 相关资源

- [LeetCode 中国](https://leetcode.cn/)
- [Go 官方文档](https://golang.org/doc/)
- [Go Testing 包](https://golang.org/pkg/testing/)
- [Effective Go](https://golang.org/doc/effective_go)

## 💡 最佳实践

1. **一题多解**：在同一个 solution.go 中实现多种解法对比
2. **详细注释**：标注时间复杂度和空间复杂度
3. **完整测试**：包含边界情况和极端情况
4. **性能测试**：使用 Benchmark 比较不同算法
5. **代码格式**：定期运行 `make fmt`

## 🎉 开始使用

1. **阅读快速开始指南**：`快速开始指南.md`
2. **查看示例题目**：`problems/0001.two-sum`
3. **创建你的第一题**：`make new NUM=x NAME=xxx`
4. **开始刷题**！

---

**祝你刷题愉快，Offer 多多！🚀**

